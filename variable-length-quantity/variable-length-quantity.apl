#!/usr/bin/env apl --script

version ← '1.0.0' ⍝ variable-length-quantity exercise, GNU-APL track, exercism

domain∆error ← {⍵ ÷ 0} ⍝ may come in handy

∇result ← encode data
    result ← 'Implement me and return a vector of integers in hex-string notation'
∇

∇result ← decode data
    result ← 'Implement me and return a vector of integers in hex-string notation'
∇
