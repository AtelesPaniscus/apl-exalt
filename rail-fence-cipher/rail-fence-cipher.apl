#!/usr/bin/env apl --script

version ← '1.0.1' ⍝ rail-fence-cipher exercise, GNU-APL track, exercism

∇result ← msg encode rails
    result ← 'Implement me and return a string'
∇

∇result ← msg decode rails
    result ← 'Implement me and return a string'
∇
