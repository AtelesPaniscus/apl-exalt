#!/usr/bin/env apl --script

version ← '2.0.1' ⍝ hamming exercise, GNU-APL track, exercism

length∆error ← {1 1 = (~⍵) ⍴ 1} ⍝ may come in handy

∇result ← strand1 distance strand2
    result ← 'Implement me and return an integer'
∇
